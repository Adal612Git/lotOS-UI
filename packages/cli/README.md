# @lotosui/cli

Command line tools for LotOS UI components, templates, themes, runtimes, desktop starters, and stack scaffolds.

## Smoke

```bash
pnpm --filter @lotosui/cli build
pnpm run verify:cli-smoke
```

The CLI reads registry-backed facts and must stay aligned with `.ai/lotos.*.json` and MCP.
