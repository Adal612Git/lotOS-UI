# No environment variables are required for install, lint, types, package tests,
# public demos, or the production build.
#
# Web auth, Supabase, checkout, and entitlement variables are documented in:
#   apps/web/.env.example
#
# Create apps/web/.env.local only when testing those external integrations.
# Keep every value local or in the deployment provider's secret store.

