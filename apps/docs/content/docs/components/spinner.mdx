# Spinner

`Spinner` communicates short loading states for async actions and deferred panels.

## Example

```tsx
import { Spinner } from '@lotosui/claude-arm';

<Spinner size="md" label="Loading incidents" />
```

## Notes

- Always provide a clear accessible label when used by itself.
- Prefer compact sizes inside buttons and controls.
