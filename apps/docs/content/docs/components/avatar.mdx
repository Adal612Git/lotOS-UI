# Avatar

`Avatar` renders user identity in lists, headers, and approvals.

## Example

```tsx
import { Avatar } from '@lotosui/claude-arm';

<Avatar name="Mia Solis" size="lg" />
```

## Notes

- Provide `alt` text when using `src`.
- Use names so fallback initials remain stable when images fail.
